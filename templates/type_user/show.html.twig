{% extends 'base.html.twig' %}

{% block title %}TypeUser{% endblock %}

{% block body %}
    <h1>TypeUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_user.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_user.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_user_index') }}">back to list</a>

    <a href="{{ path('app_type_user_edit', {'id': type_user.id}) }}">edit</a>

    {{ include('type_user/_delete_form.html.twig') }}
{% endblock %}
