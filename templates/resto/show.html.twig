{% extends 'base.html.twig' %}

{% block title %}Resto{% endblock %}

{% block body %}
    <h1>Resto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resto.id }}</td>
            </tr>
            <tr>
                <th>NomR</th>
                <td>{{ resto.nomR }}</td>
            </tr>
            <tr>
                <th>NumAdrR</th>
                <td>{{ resto.numAdrR }}</td>
            </tr>
            <tr>
                <th>RueAdrR</th>
                <td>{{ resto.rueAdrR }}</td>
            </tr>
            <tr>
                <th>CpR</th>
                <td>{{ resto.cpR }}</td>
            </tr>
            <tr>
                <th>VilleR</th>
                <td>{{ resto.villeR }}</td>
            </tr>
            <tr>
                <th>HorairesR</th>
                <td>{{ resto.horairesR }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_resto_index') }}">back to list</a>

    <a href="{{ path('app_resto_edit', {'id': resto.id}) }}">edit</a>

    {{ include('resto/_delete_form.html.twig') }}
{% endblock %}
