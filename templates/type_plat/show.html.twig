{% extends 'base.html.twig' %}

{% block title %}TypePlat{% endblock %}

{% block body %}
    <h1>TypePlat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_plat.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_plat.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_plat_index') }}">back to list</a>

    <a href="{{ path('app_type_plat_edit', {'id': type_plat.id}) }}">edit</a>

    {{ include('type_plat/_delete_form.html.twig') }}
{% endblock %}
